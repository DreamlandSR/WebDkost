apiVersion: v1
kind: Service
metadata:
  name: webdkost-service
  namespace: webdkost
spec:
  selector:
    app: webdkost-app
  type: NodePort
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080
